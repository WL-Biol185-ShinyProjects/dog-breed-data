library(shiny)

# Define UI for application that draws a histogram
fluidPage(
  
  # Application title
  titlePanel("Westminster Dog Show Data"),
  
  mainPanel(
    leafletOutput("map"))
    
    
    )
  

